void bus_write(bus_t bus, uint16_t addr, uint8_t data){
  if (addr > 0x0000 && addr <= 0xFFFF){   
    bus->ram[addr] = data;
  }
}

uint8_t bus_read(bust_t bus, uint16_t addr, uint8_t data){
  if (addr > 0x0000 && addr <= 0xFFFF){   
   return ram[addr];
  }
  return 0x0;
 }
